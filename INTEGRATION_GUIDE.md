# Руководство по интеграции новых функций

## Что было добавлено на фронтенде

### 1. Компоненты навигации
- **Navigation** (`client/components/layout/Navigation.tsx`) - главная навигация с:
  - Поиском (SearchBar)
  - Уведомлениями (NotificationCenter)
  - Ссылками на Boards, Chat, Admin (для админов)
  - Выпадающим меню пользователя
- **LayoutWrapper** (`client/components/layout/LayoutWrapper.tsx`) - обертка для условного отображения навигации

### 2. Компоненты для пользователей
- **UserProfile** (`client/components/user/UserProfile.tsx`) - страница профиля
- **NotificationCenter** (`client/components/notifications/NotificationCenter.tsx`) - центр уведомлений
- **SearchBar** (`client/components/search/SearchBar.tsx`) - глобальный поиск

### 3. Компоненты для админов
- **AnalyticsDashboard** (`client/components/admin/AnalyticsDashboard.tsx`) - аналитика
- **ActivityLog** (`client/components/admin/ActivityLog.tsx`) - журнал действий
- **UserManagementPanel** (`client/components/admin/UserManagementPanel.tsx`) - управление пользователями
- **TaskManager** (`client/components/admin/TaskManager.tsx`) - менеджер задач

### 4. Страницы
- `/profile` - профиль пользователя (`client/app/profile/page.tsx`)
- `/admin` - панель администратора (`client/app/admin/page.tsx`)

### 5. Защита маршрутов
- **ProtectedRoute** (`client/components/auth/ProtectedRoute.tsx`) - компонент для защиты страниц

## Интеграция завершена

### Навигация уже интегрирована:
✅ Layout обновлен (`client/app/layout.tsx`)
✅ Navigation автоматически отображается на всех страницах кроме `/login`, `/register`, `/`
✅ SearchBar и NotificationCenter уже в навигации
✅ Ссылки на профиль и админку добавлены в меню пользователя

### Защита маршрутов настроена:
✅ `/profile` - требует авторизации
✅ `/admin` - требует роль admin

## Использование GraphQL

### Запросы (Queries)
```typescript
import {
  NOTIFICATIONS_QUERY,
  ANALYTICS_QUERY,
  ACTIVITY_LOGS_QUERY,
  SEARCH_QUERY
} from '@/lib/graphql/queries';
```

### Мутации (Mutations)
```typescript
import {
  UPDATE_PROFILE_MUTATION,
  CHANGE_PASSWORD_MUTATION,
  MARK_NOTIFICATION_AS_READ_MUTATION,
  MARK_ALL_NOTIFICATIONS_AS_READ_MUTATION,
  DELETE_NOTIFICATION_MUTATION
} from '@/lib/graphql/mutations';
```

## Что нужно сделать на бэкенде

### Использование логирования активности

В ваших резолверах добавьте логирование:

```typescript
import { logActivityWithContext } from '../utils/activityLogger';

// Пример в резолвере создания карты
export const cardResolvers = {
  Mutation: {
    createCard: async (_: any, { input }: any, context: Context) => {
      // ... ваш код создания карты
      const card = await Card.create({...});

      // Логирование активности
      await logActivityWithContext(context, {
        userId: context.user.id,
        action: 'create',
        entityType: 'card',
        entityId: card._id,
        details: `Created card "${card.title}"`,
      });

      return card;
    },
  },
};
```

### Создание уведомлений

Используйте helper функцию для создания уведомлений:

```typescript
import { createNotification } from '../resolvers/notification.resolvers';

// Пример: отправка уведомления при комментарии
await createNotification({
  recipient: cardOwnerId,
  sender: context.user.id,
  type: 'comment',
  title: 'New comment on your card',
  message: `${context.user.name} commented on "${card.title}"`,
  entityType: 'card',
  entityId: card._id,
});
```

## Доступные страницы

После авторизации пользователь может перейти на:
- `/boards` - доски задач
- `/chat` - чат команды
- `/profile` - свой профиль
- `/admin` - админка (только для админов)

## Навигация

Навигационная панель автоматически отображается на всех страницах и включает:
- 🔍 **Поиск** - глобальный поиск по доскам и карточкам
- 🔔 **Уведомления** - с индикатором непрочитанных
- 📋 **Boards** - ссылка на доски
- 💬 **Chat** - ссылка на чат
- 👤 **Admin** - ссылка на админку (только для админов)
- **Меню пользователя** - профиль и выход

## Проверка работы

1. Запустите сервер: `npm run dev` (в папке server)
2. Запустите клиент: `npm run dev` (в папке client)
3. Войдите в систему
4. Проверьте навигацию - должны быть видны все ссылки
5. Попробуйте:
   - Поиск - введите текст в поисковую строку
   - Уведомления - кликните на иконку колокольчика
   - Профиль - через меню пользователя
   - Админку - если вы администратор

## Troubleshooting

### Навигация не отображается
- Проверьте, что вы авторизованы
- Убедитесь, что вы не на странице `/login` или `/register`

### Нет доступа к админке
- Убедитесь, что у вашего пользователя роль `admin` в базе данных
- Можно промоутить себя через MongoDB:
  ```js
  db.users.updateOne(
    { email: "your@email.com" },
    { $set: { role: "admin" } }
  )
  ```

### Ошибки GraphQL
- Убедитесь, что сервер запущен
- Проверьте консоль браузера на ошибки
- Убедитесь, что все новые резолверы экспортированы в `server/src/resolvers/index.ts`

## Что готово к использованию

Все компоненты полностью интегрированы и готовы к работе:
- ✅ Навигация с поиском и уведомлениями
- ✅ Страница профиля пользователя
- ✅ Админ-панель с аналитикой
- ✅ Журнал действий
- ✅ Система уведомлений
- ✅ Глобальный поиск
- ✅ Защита маршрутов

Просто запустите приложение и начните использовать!
